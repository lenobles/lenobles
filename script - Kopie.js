// Ajoutez une logique pour afficher les détails des recettes ici
document.querySelectorAll('.details').forEach(link => {
    link.addEventListener('click', (event) => {
        event.preventDefault();
        alert('Détails de la recette non implémentés.');
    });
});